config.yaml
# Local Go build output
backend/backend
# DB dumps
backup-charmsdb-*.sql*
.vscode/
# Notes to myself
notes.txt
# Ignore local npm modules
node_modules/*
package.json
package-lock.json
# Tag annotation script
tag.sh
# Project notes
TODO.md
# ai resume
resume.sh
resume.txt
# Production secrets
.env.prod
# OS and editor artifacts
**/.DS_Store
**/Thumbs.db
**/.claude
**/.codex
**/.trash/
**/*.swp
# Python artifacts
**/*.pyc
**/*.pyo
**/__pycache__/
# AI project worktrees. These live inside the repo, so without this rule every
# file in one shows up as untracked here and could be committed to main by
# accident — the exact mistake the worktree exists to prevent.
**/ai-wt/
